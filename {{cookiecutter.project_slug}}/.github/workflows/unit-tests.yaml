# Generated by https://github.com/Talend/lab-data-science-template

# Run tests against the installed package and make sure they all pass.
# For more information, see: https://github.com/pre-commit/action

name: unit-tests
on: [push]
jobs:
  unit-tests:
    name: unit-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8
      - name: Install development dependencies
        run: pip install -r requirements-dev.txt
      - name: Install the lab_schema_mapping package
        run: pip install .
      - name: Run unit tests
        run: pytest
