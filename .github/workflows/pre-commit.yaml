# Generated by https://github.com/anoru/data-ci-cd-template

# Run `pre-commit` on all files.
# If this action fails, you should run `pre-commit run --all-files` on your local files to fix the issues,
# and run `pre-commit install` in order to perform the checks on each commit automatically.

name: pre-commit
on: [pull_request]
jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8
      - name: Install pre-commit
        run: pip install pre-commit
      - name: Run pre-commit on all files
        run: pre-commit run --all-files --show-diff-on-failure --color=always
