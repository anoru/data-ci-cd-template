# Generated by https://github.com/Talend/lab-data-science-template

# Use Mypy and pyright to perform static type checking, and make sure no type error was introduced.

name: type-checks
on: [pull_request]
jobs:
  type-checks:
    name: type-checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8
      - name: Install dependencies
        run: pip install -r requirements-dev.txt
      - name: Run mypy
        run: mypy .
